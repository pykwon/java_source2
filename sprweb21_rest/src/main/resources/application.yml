# key: value 형식으로 작성됨 파일 확장자는 .yml
server:
  port: 80
  servlet: 
    session:
      tracking-modes: cookie
spring: 
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/test
    username: root
    password: 123
mybatis:
  mapper-locations: classpath:mapper/**/**.xml
  type-aliases-package: pack.dto.**